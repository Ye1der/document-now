import { ConstantMainCardStates } from '@/types'

export const TOKEN_KEY = 'access_token'

export const MAIN_CARD_STATES: ConstantMainCardStates = {
  close: 'close',
  info: 'info',
  create: 'create'
}
